<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= content_for?(:title) ? yield(:title) : "Oodls" %></title>
    <script src="https://maps.google.com/maps/api/js?sensor=true"></script>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= javascript_include_tag "application" %>
    <%= stylesheet_link_tag    "application" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
  <div class="sticky">
    <nav class="top-bar" data-topbar role="navigation">
      <ul class="title-area">
        <li class="name">
          <h1><a href="/">Oodls</a></h1>
        </li>
        <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
      </ul>
      <section class="top-bar-section">
        <ul class="right">
          <li class="has-dropdown">
            <a href="#">Charities</a>
            <ul class="dropdown">
              <% if charity_signed_in? %>
                <li><%= link_to "Sign Out", destroy_charity_session_path, method: :delete %></li>
              <% else %>
                <li><%= link_to "I am a charity", "/charity" %></li>
                <li><%= link_to "See all charities", "/charity" %></li>
              <% end %>
            </ul>
          </li>
        </ul>
      </section>
    </nav>
  </div>

  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>

  <%= yield %>

  </body>
</html>
